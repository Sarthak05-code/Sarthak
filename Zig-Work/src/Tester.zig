const std = @import("std");

const Scale = enum {
    Small,
    Medium,
    Large,
    ExtraLarge
};

const Rectal = struct {
    
};

pub fn main(init: std.process.Init) !void {
    _ = init;
    
}
